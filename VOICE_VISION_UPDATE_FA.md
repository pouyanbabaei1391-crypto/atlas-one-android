به‌روزرسانی محدود صدا، دوربین و دیدن صفحه

این فایل، سورس پروژهٔ Flutter است؛ فایل APK نصب‌شدنی نیست.

تغییرهای انجام‌شده
- کارت‌های تصویری بالای صفحه به کلیدهای متنی فارسی تبدیل شدند. عنوان‌ها و برچسب‌های انگلیسی رابط فارسی شدند؛ نام واقعی برنامه‌های نصب‌شده، نام مدل و نشانی سرور تغییر نمی‌کنند.
- دریافت پاسخ به‌صورت جریانی از همان سرور قبلی؛ خواندن جمله‌های کامل هم‌زمان با تولید ادامهٔ پاسخ. اگر سرور جریان یا حالت JSON را نپذیرد، مسیر پاسخ معمول امتحان می‌شود.
- مکث پیشنهادی پایان گفتار ۸۰۰ میلی‌ثانیه و مهلت نتیجهٔ نهایی ۳۵۰ میلی‌ثانیه شده است؛ سیستم‌عامل می‌تواند این زمان‌ها را متفاوت اجرا کند.
- صدای فارسی گوشی استفاده می‌شود. نبود صدای فارسی صریحاً در رابط نشان داده می‌شود. هیچ سرویس صدای جدید، کلید جدید یا اتصال خارجی دیگری اضافه نشده است.
- در پاسخ صوتی، انتظار بازیابی معنایی به ۱۵۰ میلی‌ثانیه محدود شده؛ ذخیرهٔ محلی رمزگذاری‌شده و تاریخچه باقی مانده‌اند. بازیابی کند برای همان پاسخ منتظر نمی‌ماند؛ نمایه‌سازی در پس‌زمینه ادامه می‌یابد.
- دوربین بدون ویجت پیش‌نمایش کار می‌کند. انتخاب دوربین جلو/پشت، تحلیل اولیهٔ خودکار و پاسخ صوتی اضافه شده است. عکس موقت هر نوبت پس از خواندن پاک می‌شود؛ فایل‌های شخصی کاربر تغییر نمی‌کنند.
- دوربین هنگام رفتن برنامه به پس‌زمینه آزاد و خاموش می‌شود؛ برای استفادهٔ دوباره کلید آن را روشن کنید.
- با فعال بودن هر منبع دید، فریم آن به نوبت‌های گفت‌وگو افزوده می‌شود. با فعال بودن هر دو، تصویر صفحه و دوربین هر دو ارسال می‌شوند. تحلیل دستی هر منبع فقط تصویر همان منبع را می‌فرستد.
- هنگامی که میکروفون خاموش است، تحلیل خودکار دید با فاصلهٔ حداقل چهار ثانیه پس از پایان نوبت قبلی بررسی می‌شود. هنگامی که میکروفون روشن است، تصویر همراه سؤال صوتی تحلیل می‌شود تا تحلیل خودکار مانع گوش‌دادن نشود.
- در اندروید، کدگذاری فریم صفحه به نخ جدا منتقل شده، فاصلهٔ کدگذاری به ۲۵۰ میلی‌ثانیه و کیفیت JPEG به ۸۵ تغییر کرده است. این زمان، زمان پاسخ مدل نیست.
- توقف اشتراک صفحه از اعلان سیستم اضافه شد. مجوز سیستم، اعلان و نشانگرهای میکروفون/دوربین حفظ شده‌اند.

روش استفاده
۱. پروژه را مانند قبل با BUILD_ANDROID.bat بسازید و APK حاصل را نصب کنید؛ تنظیمات سرور و مدل قبلی را نگه دارید.
۲. برای مکالمه، کلید میکروفون را روشن کنید و فارسی حرف بزنید. مدل باید از سرور تنظیم‌شده در دسترس باشد و صدای فارسی موتور گفتار دستگاه نصب/فعال باشد.
۳. برای مشاهدهٔ محیط، دوربین را روشن کنید؛ تصویر روی صفحه نمایش داده نمی‌شود. کلید جابه‌جایی، دوربین جلو و پشت را عوض می‌کند.
۴. برای دیدن صفحهٔ گوشی اندرویدی، اشتراک صفحه را تأیید کنید. اگر سیستم انتخاب می‌دهد، برای دیدن سایر برنامه‌ها «تمام صفحه» را انتخاب کنید. محتوای محافظت‌شده یا خارج از محدودهٔ اشتراک قابل دیدن نیست.
۵. با خاموش کردن کلید مربوط، دریافت آن منبع قطع می‌شود. کلید خاموشی کلی، قابلیت‌های فعال را متوقف می‌کند.

محدودیت‌های واقعی
- پاسخ کامل یا شروع صدا زیر یک ثانیه تضمین و اندازه‌گیری نشده است. تأخیر واقعی شامل پایان‌یابی گفتار سیستم، شبکه، توان مدل، پردازش تصویر و موتور گفتار است.
- کیفیت طبیعی و لهجهٔ صدا تابع صدای فارسی نصب‌شده است؛ این تغییر موتور صوتی اختصاصی مشابه ChatGPT اضافه نمی‌کند.
- مدل تنظیم‌شده باید ورودی تصویر را پشتیبانی کند. خواندن دقیق متن کوچک و پاسخ درست دربارهٔ هر تصویر تضمین‌شدنی نیست.
- این قابلیت تحلیل فریم‌های دوره‌ای است، نه پردازش تمام فریم‌های ویدئو.
- مسیر موجود iOS تغییر نکرده و ضبط ReplayKit آن محدود به خود برنامه است؛ دیدن تمام برنامه‌های آیفون به افزونهٔ پخش صفحه و تغییرات ساخت جداگانه نیاز دارد.

وضعیت بررسی
- نحو همهٔ فایل‌های Dart و Kotlin با تجزیه‌گر بررسی شد.
- مقایسهٔ بایت‌به‌بایت با ZIP ورودی انجام شد؛ فایل‌های تنظیم اتصال، پل بومی، مجوزها، اسکریپت‌های ساخت، تنظیمات مخزن، حافظه و سایر فایل‌های خارج از فهرست تغییرها دست‌نخورده‌اند.
- Flutter/Dart SDK و دستگاه اندروید در محیط بررسی موجود نبودند؛ flutter analyze، flutter test، ساخت APK، اجرای واقعی دوربین/میکروفون و سنجش تأخیر اجرا نشده‌اند.
- فایل test/reply_stream_test.dart آزمون مرزبندی قطعه‌های شبکه، فارسی، نویسه‌های escape، جلوگیری از خواندن JSON فرمان‌ها و تکرار صدا را دارد. پس از flutter pub get با flutter test test/reply_stream_test.dart اجرا کنید.
- VERIFY_PROJECT.py موجود، به‌دلیل نبود دو فایل .github/workflows/build-android-apk.yml و .github/workflows/validate-ios.yml خطا می‌دهد. این دو فایل در ZIP ورودی هم وجود نداشتند؛ طبق محدودیت تغییرات، ساخت و اتصال مخزن اصلاح نشده‌اند.

مراجع APIهای استفاده‌شده
https://pub.dev/packages/speech_to_text
https://pub.dev/packages/flutter_tts
