name: Build Atlas Android Slim Qwen3 APK

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: atlas-android-slim-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-slim-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@v5

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Test and build Slim local Qwen3 edition
        env:
          ATLAS_BUNDLE_MODEL: 'false'
        run: python3 english_voice_addon/build.py android

      - name: Enforce Slim installer ceiling
        run: python3 english_voice_addon/verify_slim_apk.py dist/Atlas-One-Android.apk --max-bytes 800000000

      - name: Upload verified Slim APK
        uses: actions/upload-artifact@v5
        with:
          name: Atlas-One-Android-SLIM-QWEN3-APK
          path: dist/Atlas-One-Android.apk
          if-no-files-found: error
          compression-level: 0
